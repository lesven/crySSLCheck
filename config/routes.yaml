controllers:
    resource:
        path: ../src/Controller/
        namespace: App\Controller
    type: attribute

# Root-URL auf Domain-Index weiterleiten
app_root:
    path: /
    controller: Symfony\Bundle\FrameworkBundle\Controller\RedirectController::redirectAction
    defaults:
        route: domain_index
        permanent: false
