# Diese Datei wird von Docker Compose automatisch als Override geladen.
# Für lokale Entwicklung kann hier z.B. ein Mailpit-Container aktiviert werden:
#
# services:
#   mailer:
#     image: axllent/mailpit
#     ports:
#       - "1025:1025"
#       - "8025:8025"
#     environment:
#       MP_SMTP_AUTH_ACCEPT_ANY: 1
#       MP_SMTP_AUTH_ALLOW_INSECURE: 1
#
# Danach in .env setzen:
#   MAILER_DSN=smtp://mailer:1025
